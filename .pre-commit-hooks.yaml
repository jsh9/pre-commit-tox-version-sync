- id: sync-precommit-tox-versions
  name: Sync pre-commit and tox dependency versions
  description: Pin selected tox deps to matching pre-commit repo revs.
  entry: sync-precommit-tox-versions
  language: python
  pass_filenames: false
  always_run: true
